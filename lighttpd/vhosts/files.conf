
$HTTP["host"] == "files.uesp.net" {
	server.document-root = "/shared/uesp/wikiimages/"
	expire.url = ( "" => "access plus 1 months" )
	var.server_name = "files1.uesp.net"
	server.name = server_name
}

